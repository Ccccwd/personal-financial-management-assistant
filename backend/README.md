# 智能个人财务记账系统 - 后端

## 简介

基于 FastAPI 构建的智能个人财务记账系统后端服务。

## 快速开始

```bash
# 安装依赖
uv sync

# 启动开发服务器
uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

## API 文档

启动服务后访问：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
